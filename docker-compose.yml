version: "2"
services:
  app:
    container_name: website-api
    restart: always
    build: .
    ports:
      - "8080:8080"
    links:
      - mongo:3.5
  mongo:
    container_name: mongo
    image: mongo:3.5
    volumes:
      - ./data:/data/db
    ports:
      - "127.0.0.1:27017:27017"
